<%= provide :edit_user, "active" %>

<div class="container w-50">
  <h2>Editar cuenta</h2>

  <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put, class: "form-group" }) do |f| %>
    <%= render "users/shared/error_messages", resource: resource %>

    <div class="field">
      <%= f.label :name %><br />
      <%= f.text_field :name, autofocus: true, autocomplete: "name", class: "form-control" %>
    </div>

    <div class="field">
      <%= f.label :profile_pic %><br />
      <%= f.file_field :profile_pic, class: "form-control" %>
    </div>

    <div class="field">
      <%= f.label :email %><br />
      <%= f.email_field :email, autocomplete: "email", class: "form-control" %>
    </div>

    <div class="field">
      <%= f.label :phone_number, "Número de celular" %><br />
      <%= f.text_field :phone_number, autocomplete: "Número de celular", class: "form-control" %>
    </div>


    <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
      <div>Actualmente esperando confirmación para: <%= resource.unconfirmed_email %></div>
    <% end %>

    <div class="field">
      <%= f.label :password %> <i>(dejar en blanco si no desea cambiarla)</i><br />
      <%= f.password_field :password, autocomplete: "new-password", class: "form-control" %>
      <% if @minimum_password_length %>
        <em><%= @minimum_password_length %> caracteres mínimo</em>
      <% end %>
    </div>

    <div class="field">
      <%= f.label :password_confirmation %><br />
      <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "form-control" %>
    </div>

    <div class="field">
      <%= f.label :current_password, "Contaraseña Actual" %> <i>(necesitamos tu contraseña actual para guardar los cambios)</i><br />
      <%= f.password_field :current_password, autocomplete: "current-password", class: "form-control" %>
    </div>

    <div class="text-center actions">
      <%= f.submit "Enviar", class: "btn btn-outline-dark" %>
    </div>
  <% end %>

  <div class="border border-danger rounded px-4 py-3 my-5">
    <h3>Cancelar mi cuenta</h3>
    <p>PRECAUCIÓN: Presionando el botón de abajo eliminará todo registro suyo en está página.</p>
    <%= button_to "QUIERO CANCELAR MI CUENTA", registration_path(resource_name), data: { confirm: "¿Estás seguro de que quieres cancelar tu cuenta?" }, method: :delete, class: "btn btn-danger" %>
  </div>

  <%= link_to "Volver a la página principal", root_path, class: "d-block w-50 btn btn-dark mx-auto my-4 font-weight-bold" %>
</div>
